#!/bin/bash
#the line above show the bin that will run the script
clear
#the line above declares a shell command that clears the screen
echo "Hello, world!"
#the line above writes a mensagem on the screen
#the line below uses variable to store a string
message="Hi, my friend!"
echo "The message is: $message"
